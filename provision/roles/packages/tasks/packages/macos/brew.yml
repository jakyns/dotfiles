---
- name: update homebrew
  homebrew:
    update_homebrew: yes

- name: upgrade homebrew packages
  homebrew:
    upgrade_all: yes

- name: install homebrew packages
  homebrew:
    name: "{{ brew_packages }}"
    state: present
