---
- name: install node and yarn
  homebrew: name={{ node_packages }} state=present

- name: install npm packages
  npm:
    name: "{{ item }}"
    state: present
    global: true
  with_items: "{{ npm_packages }}"
